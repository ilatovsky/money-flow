type Account {
	id: ID!
	name: String!
	transactions: [Transaction!]! @excludeFieldFromModel
	amount: Float! @excludeFieldFromModel
}

type AccountQuery {
	all: [Account!]!
}

extend type Query {
	account: AccountQuery!
}