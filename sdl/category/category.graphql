type Category {
	id: ID!
	name: String!
	transactions: [Transaction!]! @excludeFieldFromModel
	turnover: Float! @excludeFieldFromModel
}

type CategoryQuery {
	all: [Category!]!
}

extend type Query {
	category: CategoryQuery!
}